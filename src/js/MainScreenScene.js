import Phaser from "phaser"

class MainScreenScene extends Phaser.Scene {
	constructor(mapFromConfig) {
		super({ key: "main-screen-scene" });
	}

	preload() {

	}
	create() {

	}
	update(time, delta) {

	}
}

export { MainScreenScene };